# AI Lesson Recorder Frontend

Phase-1 mock frontend for the teacher review workspace.

## Run

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:5173`.

## Scope

This process owns only `frontend/`.

